export namespace PathfindingConfig {
    /** Maximum distance when searching for stops in walking distance (m) */
    export let walkingDistance = 150;
    /** Multiplier of heuristic for A* algorithm */
    export let heuristicWeight = 1;
}
